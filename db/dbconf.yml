development:
    driver: postgres
    open: dbname=dberror sslmode=disable

circle:
    driver: postgres
    open: user=ubuntu dbname=circle_test sslmode=disable

travis:
    driver: postgres
    open: user=postgres dbname=dberror sslmode=disable
